So I believe the connection for every single tile I think that needs to be a terminal not the direct Codex CLI because this will allow us to go beyond just Codex. Because this will allow us to go beyond just Codex, because then the user can...... the user can... the user can decide what kind of a coding platform they're going to use.
The tiles should should resize as the main center window gets bigger or smaller. The tiles should just get bigger and smaller so people can see more text because somebody could just put this as a full screen and right now we have a lot of space that it's not being used. You could see in the red we have a lot space that is not used where we should if we make this You know full screen 
Also not being able to pace an image directly to the message this session that needs to be fixed.