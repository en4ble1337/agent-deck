Product direction: clean terminal multiplexer, not decorative agent tiles.

- Each workspace/session pane should behave like a terminal-backed surface first, with Codex as one possible tool instead of the hard-coded center of the model.
- The home grid should resize panes with the available center area so full-screen users get larger readable terminal output instead of unused whitespace.
- The visual theme should be fast, quiet, dense, and utilitarian: minimal gradients, restrained borders, readable status, and no board-game/deck language.
- Pasting images directly into a session message must work consistently.


- Okay so the idea for retrofitting this application would be that we would have like a master perhaps like an orchestrator LLLM that can distribute work according to how we tell it to. on the side panel. So you could ask it to like under this workspace open additional you know two terminals to agents and do XYZ tasks so it could actually delegate multiple tasks for it instead of you to even typing directly to those boxes so you could pretty much talking to like the the distribution talking to like the distribution orchestrator agent. They also need to connect using API or self hosted and then this guy just kind of distributes work for you. So it can also like verify work, all that stuff. This is a much more practical application of the original concept, don't you think? 